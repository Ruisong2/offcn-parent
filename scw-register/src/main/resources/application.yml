server:
  port: 8761
spring:
  application:
    name: SCW-REGISTER
eureka:
  instance:
    hostname: localhost #ÓòÃû
  client:
    service-url:
      defalutZone: http://${eureka.instance.hostname}:${server.port}/eureka
    register-with-eureka: false #µ±Ç°eureka
    fetch-registry: false
